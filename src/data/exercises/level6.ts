import { defineExercise } from './helpers'
import type { Exercise } from '../../types'

export const level6Exercises: Exercise[] = [
  defineExercise({
    id: 'l6-01',
    level: 6,
    order: 1,
    title: 'Orders per month',
    difficultyScore: 4,
    description: 'Return the number of orders placed each calendar month, as `month` (truncated to the first of the month) and `order_count`.',
    tablesInvolved: ['orders'],
    conceptTags: ['DATE_TRUNC'],
    hints: [{ order: 1, text: "DATE_TRUNC('month', timestamp) rounds a timestamp down to the first moment of its month." }],
    solution: { sql: "SELECT DATE_TRUNC('month', order_date) AS month, COUNT(*) AS order_count FROM orders GROUP BY DATE_TRUNC('month', order_date);", explanation: 'DATE_TRUNC is PostgreSQL\'s general-purpose date rounding function — it also supports \'day\', \'year\', \'quarter\', \'hour\', and more.', conceptsUsed: ['DATE_TRUNC', 'GROUP BY'] },
  }),
  defineExercise({
    id: 'l6-02',
    level: 6,
    order: 2,
    title: 'Customers by signup year',
    difficultyScore: 3,
    description: 'Return the number of customers who signed up in each year, as `signup_year` and `customer_count`.',
    tablesInvolved: ['customers'],
    conceptTags: ['EXTRACT'],
    hints: [{ order: 1, text: "EXTRACT(YEAR FROM date_column) pulls the year out as a number." }],
    solution: { sql: 'SELECT EXTRACT(YEAR FROM signup_date) AS signup_year, COUNT(*) AS customer_count FROM customers GROUP BY EXTRACT(YEAR FROM signup_date);', explanation: 'EXTRACT works on any date/time field: YEAR, MONTH, DAY, DOW (day of week), HOUR, and more.', conceptsUsed: ['EXTRACT', 'GROUP BY'] },
  }),
  defineExercise({
    id: 'l6-03',
    level: 6,
    order: 3,
    title: 'Orders in a 30-day window',
    difficultyScore: 3,
    description: "Return `order_number` and `order_date` for orders placed within 30 days after 2025-06-01 (i.e. from 2025-06-01 up to, but not including, 2025-07-01).",
    tablesInvolved: ['orders'],
    conceptTags: ['INTERVAL'],
    hints: [{ order: 1, text: "date '2025-06-01' + INTERVAL '30 days' computes the end of the window." }],
    solution: {
      sql: "SELECT order_number, order_date FROM orders WHERE order_date >= '2025-06-01' AND order_date < DATE '2025-06-01' + INTERVAL '30 days';",
      explanation: 'INTERVAL values can be added directly to dates and timestamps, making relative date math explicit and readable.',
      conceptsUsed: ['INTERVAL'],
    },
  }),
  defineExercise({
    id: 'l6-04',
    level: 6,
    order: 4,
    title: 'Slow ticket resolutions',
    difficultyScore: 4,
    description: 'List the `id` of support tickets that took more than 48 hours to resolve (only consider tickets that have been resolved).',
    tablesInvolved: ['support_tickets'],
    conceptTags: ['INTERVAL', 'timestamp arithmetic'],
    hints: [{ order: 1, text: 'Subtracting two timestamps produces an interval you can compare against another interval.' }],
    solution: { sql: "SELECT id FROM support_tickets WHERE resolved_at IS NOT NULL AND resolved_at - created_at > INTERVAL '48 hours';", explanation: 'Timestamp subtraction yields an interval, which can be compared directly to a literal interval like \'48 hours\'.', conceptsUsed: ['INTERVAL', 'timestamp arithmetic'] },
  }),
  defineExercise({
    id: 'l6-05',
    level: 6,
    order: 5,
    title: 'SKUs ending in 4 digits',
    difficultyScore: 3,
    description: 'List the `sku` of every product whose SKU ends with exactly 4 digits, using a regular expression.',
    tablesInvolved: ['products'],
    conceptTags: ['regex'],
    hints: [{ order: 1, text: "The `~` operator matches a POSIX regular expression. `[0-9]{4}$` means 'exactly 4 digits at the end'." }],
    solution: { sql: "SELECT sku FROM products WHERE sku ~ '[0-9]{4}$';", explanation: '`~` performs case-sensitive regex matching in PostgreSQL; `~*` is the case-insensitive variant, and `!~` / `!~*` negate them.', conceptsUsed: ['regex'] },
  }),
  defineExercise({
    id: 'l6-06',
    level: 6,
    order: 6,
    title: 'Senior job titles',
    difficultyScore: 3,
    description: "List `first_name`, `last_name`, and `title` for employees whose title contains 'senior', in any capitalization.",
    tablesInvolved: ['employees'],
    conceptTags: ['regex', 'case-insensitive'],
    hints: [{ order: 1, text: '`~*` is the case-insensitive regex match operator.' }],
    solution: { sql: "SELECT first_name, last_name, title FROM employees WHERE title ~* 'senior';", explanation: '`~*` ignores case, so it matches \'Senior\', \'SENIOR\', and \'senior\' alike — no substring anchors are needed since the pattern has no `^`/`$`.', conceptsUsed: ['regex'] },
  }),
  defineExercise({
    id: 'l6-07',
    level: 6,
    order: 7,
    title: 'Heavy products from JSONB',
    difficultyScore: 4,
    description: "Products store their weight as text inside the `attributes` JSONB column (key `weightKg`). Return `sku` and a numeric `weight_kg` column for products heavier than 10 kg.",
    tablesInvolved: ['products'],
    conceptTags: ['JSONB', 'casting'],
    hints: [
      { order: 1, text: "attributes->>'weightKg' extracts the value as text." },
      { order: 2, text: 'Cast that text to numeric with ::numeric before comparing it to 10.' },
    ],
    solution: {
      sql: "SELECT sku, (attributes->>'weightKg')::numeric AS weight_kg FROM products WHERE (attributes->>'weightKg')::numeric > 10;",
      explanation: "`->>` extracts a JSON field as text (vs. `->` which keeps it as jsonb); casting with `::numeric` makes it usable in numeric comparisons.",
      conceptsUsed: ['JSONB', 'casting'],
    },
    validation: { roundDecimals: 2 },
  }),
  defineExercise({
    id: 'l6-08',
    level: 6,
    order: 8,
    title: 'Bestseller products',
    difficultyScore: 3,
    description: "List the `name` of every product tagged 'bestseller' in its `tags` array.",
    tablesInvolved: ['products'],
    conceptTags: ['arrays', 'containment'],
    hints: [{ order: 1, text: "The `@>` operator checks whether the array on the left contains the array on the right: tags @> ARRAY['bestseller']." }],
    solution: { sql: "SELECT name FROM products WHERE tags @> ARRAY['bestseller'];", explanation: '`@>` is the array (and JSONB) containment operator — it reads naturally as "tags contains [\'bestseller\']".', conceptsUsed: ['arrays'] },
  }),
  defineExercise({
    id: 'l6-09',
    level: 6,
    order: 9,
    title: 'Employees who know Python',
    difficultyScore: 3,
    description: "List `first_name` and `last_name` of employees whose `skills` array includes 'python'.",
    tablesInvolved: ['employees'],
    conceptTags: ['arrays', 'ANY'],
    hints: [{ order: 1, text: "'python' = ANY(skills) checks membership in the array." }],
    solution: { sql: "SELECT first_name, last_name FROM employees WHERE 'python' = ANY(skills);", explanation: 'ANY(array) expands the array into a set of values that the left-hand expression is compared against with `=` — true if any element matches.', conceptsUsed: ['arrays', 'ANY'] },
  }),
  defineExercise({
    id: 'l6-10',
    level: 6,
    order: 10,
    title: 'SKUs per order, as an array',
    difficultyScore: 4,
    description: 'For every order, return `order_id` and an array `skus` containing the SKU of every product in that order, sorted alphabetically.',
    requirements: ['The array must be sorted alphabetically by SKU — use ORDER BY inside the aggregate function.'],
    tablesInvolved: ['order_items', 'products'],
    conceptTags: ['ARRAY_AGG', 'JOIN'],
    hints: [{ order: 1, text: 'ARRAY_AGG(expr ORDER BY expr) sorts the values before collecting them into an array — without it, the order is undefined.' }],
    solution: {
      sql: `SELECT oi.order_id, ARRAY_AGG(p.sku ORDER BY p.sku) AS skus
FROM order_items oi
JOIN products p ON p.id = oi.product_id
GROUP BY oi.order_id;`,
      explanation: 'ARRAY_AGG collects grouped values into an array; the ORDER BY inside the function call (distinct from any outer ORDER BY) controls the order of elements within each array.',
      conceptsUsed: ['ARRAY_AGG', 'GROUP BY'],
    },
  }),
  defineExercise({
    id: 'l6-11',
    level: 6,
    order: 11,
    title: 'Black products',
    difficultyScore: 2,
    description: "List `sku` and `name` of products whose `attributes` JSONB has `color` equal to 'black'.",
    tablesInvolved: ['products'],
    conceptTags: ['JSONB', '->>'],
    hints: [{ order: 1, text: "attributes->>'color' returns the color field as text." }],
    solution: { sql: "SELECT sku, name FROM products WHERE attributes->>'color' = 'black';", explanation: "The `->>` operator is the most common way to filter on a scalar JSONB field.", conceptsUsed: ['JSONB'] },
  }),
  defineExercise({
    id: 'l6-12',
    level: 6,
    order: 12,
    title: 'Customers acquired through referral',
    difficultyScore: 3,
    description: "List the `email` of customers whose `metadata` JSONB contains `acquisitionChannel: \"referral\"`, using the JSONB containment operator.",
    tablesInvolved: ['customers'],
    conceptTags: ['JSONB', 'containment'],
    hints: [{ order: 1, text: "metadata @> '{\"acquisitionChannel\": \"referral\"}'::jsonb checks that the JSON object is a subset of metadata." }],
    solution: { sql: `SELECT email FROM customers WHERE metadata @> '{"acquisitionChannel": "referral"}'::jsonb;`, explanation: '`@>` works on JSONB the same way it works on arrays: it tests whether the right-hand document\'s keys/values are all present in the left-hand one.', conceptsUsed: ['JSONB', 'containment'] },
  }),
  defineExercise({
    id: 'l6-13',
    level: 6,
    order: 13,
    title: 'All ticket subjects per customer, as JSON',
    difficultyScore: 4,
    description: 'For every customer who has at least one support ticket, return `customer_id` and a `subjects` column: a JSON array of their ticket subjects, ordered by ticket creation time.',
    requirements: ['Order the subjects within the array by created_at.'],
    tablesInvolved: ['support_tickets'],
    conceptTags: ['JSONB_AGG', 'JSON aggregation'],
    hints: [{ order: 1, text: 'JSONB_AGG(expr ORDER BY ...) works just like ARRAY_AGG but produces a JSON array.' }],
    solution: {
      sql: 'SELECT customer_id, JSONB_AGG(subject ORDER BY created_at) AS subjects FROM support_tickets GROUP BY customer_id;',
      explanation: 'JSONB_AGG (and its cousin JSON_AGG) turn a grouped column into a JSON array — useful whenever an API response needs nested arrays out of flat rows.',
      conceptsUsed: ['JSONB_AGG', 'GROUP BY'],
    },
  }),
  defineExercise({
    id: 'l6-14',
    level: 6,
    order: 14,
    title: 'Daily order counts for June 2025, including zero-order days',
    difficultyScore: 5,
    description: 'Using `generate_series` to build a full calendar of every day in June 2025, return `day` and `order_count` for each day — days with no orders must still appear, with a count of 0.',
    tablesInvolved: ['orders'],
    conceptTags: ['generate_series', 'LEFT JOIN'],
    hints: [
      { order: 1, text: "generate_series('2025-06-01'::date, '2025-06-30'::date, interval '1 day') produces one row per day." },
      { order: 2, text: 'LEFT JOIN the real orders onto this date spine so days without orders are preserved.' },
    ],
    solution: {
      sql: `WITH days AS (
  SELECT generate_series('2025-06-01'::date, '2025-06-30'::date, interval '1 day')::date AS day
)
SELECT d.day, COUNT(o.id) AS order_count
FROM days d
LEFT JOIN orders o ON o.order_date::date = d.day
GROUP BY d.day;`,
      explanation: 'generate_series builds a "date spine" — a common technique so that days/months/hours with zero activity still show up in a report, which a plain GROUP BY on orders alone would never produce.',
      conceptsUsed: ['generate_series', 'LEFT JOIN', 'date spine'],
    },
  }),
  defineExercise({
    id: 'l6-15',
    level: 6,
    order: 15,
    title: "Each customer's most recent order",
    difficultyScore: 4,
    description: 'Return one row per customer: their `customer_id`, most recent `order_date`, and its `status` — only the latest order per customer.',
    tablesInvolved: ['orders'],
    conceptTags: ['DISTINCT ON'],
    hints: [
      { order: 1, text: 'DISTINCT ON (col) keeps the first row per distinct value of col, based on the query\'s ORDER BY.' },
      { order: 2, text: 'ORDER BY customer_id, order_date DESC ensures the "first" row per customer is the most recent order.' },
    ],
    solution: {
      sql: 'SELECT DISTINCT ON (customer_id) customer_id, order_date, status FROM orders ORDER BY customer_id, order_date DESC;',
      explanation: 'DISTINCT ON is a PostgreSQL-specific shortcut for "top 1 per group" that avoids a window function or correlated subquery — but it depends entirely on the ORDER BY to determine which row is kept.',
      conceptsUsed: ['DISTINCT ON'],
    },
  }),
  defineExercise({
    id: 'l6-16',
    level: 6,
    order: 16,
    title: 'Weekend order volume',
    difficultyScore: 3,
    description: 'Return the total number of orders placed on a Saturday or Sunday, as `weekend_orders`.',
    tablesInvolved: ['orders'],
    conceptTags: ['EXTRACT', 'DOW'],
    hints: [{ order: 1, text: "EXTRACT(DOW FROM timestamp) returns 0 for Sunday through 6 for Saturday." }],
    solution: { sql: 'SELECT COUNT(*) AS weekend_orders FROM orders WHERE EXTRACT(DOW FROM order_date) IN (0, 6);', explanation: 'DOW (day of week) is one of the field names EXTRACT understands, alongside YEAR, MONTH, DAY, HOUR, DOY (day of year), and more.', conceptsUsed: ['EXTRACT'] },
  }),
  defineExercise({
    id: 'l6-17',
    level: 6,
    order: 17,
    title: 'Orders per quarter',
    difficultyScore: 3,
    description: 'Return the number of orders placed in each calendar quarter, as `order_quarter` and `order_count`.',
    tablesInvolved: ['orders'],
    conceptTags: ['EXTRACT'],
    hints: [{ order: 1, text: 'EXTRACT(QUARTER FROM timestamp) returns 1 through 4.' }],
    solution: { sql: 'SELECT EXTRACT(QUARTER FROM order_date) AS order_quarter, COUNT(*) AS order_count FROM orders GROUP BY EXTRACT(QUARTER FROM order_date);', explanation: 'QUARTER is one of the less common EXTRACT fields, but works exactly like YEAR or MONTH — it just buckets months 1-3, 4-6, 7-9, and 10-12 into 1-4.', conceptsUsed: ['EXTRACT', 'GROUP BY'] },
  }),
  defineExercise({
    id: 'l6-18',
    level: 6,
    order: 18,
    title: 'Tenure of former employees',
    difficultyScore: 3,
    description: 'For every employee who has left the company, return `first_name`, `last_name`, and `tenure` — the AGE() between their `termination_date` and `hire_date`.',
    tablesInvolved: ['employees'],
    conceptTags: ['AGE', 'date functions'],
    hints: [{ order: 1, text: 'AGE(later_date, earlier_date) returns a human-readable interval like "2 years 3 mons 10 days".' }],
    solution: {
      sql: "SELECT first_name, last_name, AGE(termination_date, hire_date) AS tenure FROM employees WHERE termination_date IS NOT NULL;",
      explanation: 'AGE() subtracts two dates and formats the result in calendar-aware years/months/days, which is usually far more readable than a raw day count.',
      conceptsUsed: ['AGE'],
    },
  }),
  defineExercise({
    id: 'l6-19',
    level: 6,
    order: 19,
    title: 'One row per employee skill',
    difficultyScore: 3,
    description: "Expand each employee's `skills` array into one row per skill. Return `first_name`, `last_name`, and `skill`.",
    tablesInvolved: ['employees'],
    conceptTags: ['UNNEST', 'arrays'],
    hints: [{ order: 1, text: 'UNNEST(array_column) can be used directly in the SELECT list, or joined against as a set-returning function.' }],
    solution: {
      sql: 'SELECT first_name, last_name, UNNEST(skills) AS skill FROM employees;',
      explanation: 'UNNEST turns an array column into a set of rows — an employee with 4 skills contributes 4 output rows, one per skill, with the other selected columns repeated on each.',
      conceptsUsed: ['UNNEST', 'arrays'],
    },
  }),
  defineExercise({
    id: 'l6-20',
    level: 6,
    order: 20,
    title: 'Employees with more than 3 skills',
    difficultyScore: 3,
    description: 'List `first_name`, `last_name`, and `skills` for employees whose `skills` array has more than 3 elements.',
    tablesInvolved: ['employees'],
    conceptTags: ['array_length', 'arrays'],
    hints: [{ order: 1, text: 'array_length(array_column, 1) returns the number of elements in a one-dimensional array.' }],
    solution: {
      sql: 'SELECT first_name, last_name, skills FROM employees WHERE array_length(skills, 1) > 3;',
      explanation: 'The second argument to array_length is the array dimension to measure — 1 for an ordinary one-dimensional array like skills.',
      conceptsUsed: ['array_length', 'arrays'],
    },
  }),
  defineExercise({
    id: 'l6-21',
    level: 6,
    order: 21,
    title: 'Employees who work remotely',
    difficultyScore: 4,
    description: "Employees have a `remote` boolean stored as JSON inside `metadata`. List `first_name` and `last_name` for employees where that flag is true.",
    tablesInvolved: ['employees'],
    conceptTags: ['JSONB', 'casting'],
    hints: [
      { order: 1, text: "metadata->>'remote' extracts the JSON boolean as text — it comes back as the string 'true' or 'false', not a real boolean." },
      { order: 2, text: 'Cast that text to boolean with ::boolean before comparing it to true.' },
    ],
    solution: {
      sql: `SELECT first_name, last_name FROM employees WHERE (metadata->>'remote')::boolean = true;`,
      explanation: '`->>` always returns text, even when the underlying JSON value is a boolean or number — a cast is required to use it as anything other than a string.',
      conceptsUsed: ['JSONB', 'casting'],
    },
  }),
  defineExercise({
    id: 'l6-22',
    level: 6,
    order: 22,
    title: 'New arrivals or staff picks',
    difficultyScore: 3,
    description: "List the `name` and `tags` of products tagged 'new-arrival' OR 'staff-pick' (or both), using the array overlap operator.",
    tablesInvolved: ['products'],
    conceptTags: ['arrays', 'overlap'],
    hints: [{ order: 1, text: "The `&&` operator checks whether two arrays share at least one element in common: tags && ARRAY['new-arrival', 'staff-pick']." }],
    solution: {
      sql: `SELECT name, tags FROM products WHERE tags && ARRAY['new-arrival', 'staff-pick'];`,
      explanation: '`&&` (overlap) is true if the two arrays share any element at all, unlike `@>` (containment) which requires every element on the right to be present.',
      conceptsUsed: ['arrays', 'overlap'],
    },
  }),
  defineExercise({
    id: 'l6-23',
    level: 6,
    order: 23,
    title: 'Product counts by $50 price band',
    difficultyScore: 5,
    description: "Using generate_series, build every $50 price band from $0 up to (but not including) $500. Return `band_start` and `product_count` for each band — bands with no products must still appear, with a count of 0.",
    tablesInvolved: ['products'],
    conceptTags: ['generate_series', 'LEFT JOIN'],
    hints: [
      { order: 1, text: 'generate_series(0, 450, 50) produces the 10 starting points: 0, 50, 100, ..., 450.' },
      { order: 2, text: 'LEFT JOIN products on price >= band_start AND price < band_start + 50, then GROUP BY band_start.' },
    ],
    solution: {
      sql: `SELECT band_start, COUNT(p.id) AS product_count
FROM generate_series(0, 450, 50) AS band_start
LEFT JOIN products p ON p.price >= band_start AND p.price < band_start + 50
GROUP BY band_start
ORDER BY band_start;`,
      explanation: 'This is the same "numeric spine" idea as a date spine, just built over prices instead of dates — generate_series manufactures every band up front so empty ones are not silently dropped by the join.',
      conceptsUsed: ['generate_series', 'LEFT JOIN'],
    },
  }),
  defineExercise({
    id: 'l6-24',
    level: 6,
    order: 24,
    title: 'Latest message per ticket, per sender',
    difficultyScore: 4,
    description: "For every (ticket, sender) pair, return the ticket_id, sender, body, and sent_at of that sender's most recent message on that ticket.",
    tablesInvolved: ['ticket_messages'],
    conceptTags: ['DISTINCT ON'],
    hints: [{ order: 1, text: 'DISTINCT ON accepts more than one column — DISTINCT ON (ticket_id, sender) keeps the first row for each unique combination of the two.' }],
    solution: {
      sql: `SELECT DISTINCT ON (ticket_id, sender) ticket_id, sender, body, sent_at
FROM ticket_messages
ORDER BY ticket_id, sender, sent_at DESC;`,
      explanation: 'The ORDER BY must start with the exact same columns listed in DISTINCT ON (here, ticket_id then sender) before any tiebreaking column — sent_at DESC is what makes "most recent" the row that gets kept.',
      conceptsUsed: ['DISTINCT ON'],
    },
  }),
]
